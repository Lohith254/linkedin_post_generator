# Core Libraries
torch>=2.1.0
transformers>=4.40.0
datasets>=2.19.0

# For LoRA fine-tuning (optional)
peft>=0.9.0
accelerate>=0.27.0
bitsandbytes>=0.42.0

# Web Interface (optional)
streamlit>=1.33.0

# CPU-only fallback (optional, remove if using GPU)
sentencepiece>=0.1.99